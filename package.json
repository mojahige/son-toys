{
  "name": "son-toys",
  "author": "mojahige (Yu Nozato)",
  "private": true,
  "scripts": {
    "dev": "nue serve --root my-app",
    "build": "nue build --root my-app --production",
    "preview": "nue serve --root my-app --production",
    "stats": "nue stats --root my-app",
    "format": "prettier --write 'my-app/**/*.(html|css)'"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.6.1",
    "nuekit": "^0.5.1",
    "prettier": "^3.2.5",
    "stylelint": "^16.2.1",
    "stylelint-config-standard": "^36.0.0"
  }
}
